- args:
    cascade: false
    read_only: false
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."command"("id" uuid NOT NULL DEFAULT gen_random_uuid(),
      "run_after" uuid NOT NULL, PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: command
    schema: public
  type: add_existing_table_or_view
