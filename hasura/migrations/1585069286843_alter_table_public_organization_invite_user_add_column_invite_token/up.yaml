- args:
    cascade: false
    read_only: false
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    cascade: false
    read_only: false
    sql: ALTER TABLE "public"."organization_invite_user" ADD COLUMN "invite_token"
      uuid NOT NULL DEFAULT gen_random_uuid();
  type: run_sql
