- args:
    cascade: false
    read_only: false
    sql: |-
      alter table "public"."command"
                 add constraint "command_project_id_fkey"
                 foreign key ("project_id")
                 references "public"."project"
                 ("id") on update restrict on delete cascade;
  type: run_sql
