- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."project"("id" serial NOT NULL, "name" varchar NOT
      NULL, "organization" integer NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("organization")
      REFERENCES "public"."organization"("id") ON UPDATE restrict ON DELETE restrict,
      UNIQUE ("name", "organization"));
  type: run_sql
- args:
    name: project
    schema: public
  type: add_existing_table_or_view
