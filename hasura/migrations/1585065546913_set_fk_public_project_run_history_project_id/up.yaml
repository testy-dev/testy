- args:
    cascade: false
    read_only: false
    sql: |-
      alter table "public"."project_run_history"
                 add constraint "project_run_history_project_id_fkey"
                 foreign key ("project_id")
                 references "public"."project"
                 ("id") on update restrict on delete cascade;
  type: run_sql
