- args:
    cascade: false
    read_only: false
    sql: |-
      alter table "public"."organization_invite_user"
                 add constraint "organization_invite_user_organization_id_fkey"
                 foreign key ("organization_id")
                 references "public"."organization"
                 ("id") on update restrict on delete cascade;
  type: run_sql
